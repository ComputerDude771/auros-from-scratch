
Does it install AurOS with no memory stick, and leave Windows intact?

  a 3 GiB machine: ESP, a 1 GiB Windows with 7 files, WinRE at the end
  and a 334 MiB AurOS image, which is going to live inside Windows
    AurBridge (no stick) arms the machine                        ok
    ...and wrote the image's manifest beside it                  ok
    ...and what she chose, on the EFI partition for first boot   ok
    ...and a journal that says the image is on Windows           ok
    the image is inside Windows (9 files there now)              ok

  refusals, with the disk untouched (so the read-only mount wrote nothing)
    no image in the AurOS folder: refused                        ok
    ...and the disk is byte-for-byte unchanged                   ok
    a damaged image in the AurOS folder: refused                 ok
    ...and the disk is byte-for-byte unchanged                   ok

  and then installing, for real, with no stick
    it installs and the new system starts                        ok
    ...the way back went onto the disk before AurOS did          ok
    ...in that order                                             ok
    the installer reported success                               ok

  and what it left behind
    another tool agrees the new table is valid                   ok
      p1 2048..206847 EFI
      p2 206848..1005712 Windows
      p3 5498880..6291422 WinRE
      p4 1007616..5193727 AUROS-ROOT
      p5 5193728..5400575 AUROS-SAVED
      p6 5400576..5498879 AUROS-BOOT
    the way back has a partition of its own on this disk         ok
    Windows is smaller than it was                               ok
    all 9 files in Windows, the AurOS folder too, are unchanged  ok

  and putting Windows back, still with no stick
    it restores from the copy on this computer                   ok
    ...using the copy on this computer's own disk                ok
    the three original partitions are back                       ok
    Windows' partition is its full size again                    ok
    the EFI partition is byte-for-byte what it was               ok
    the Windows filesystem is its full size again                ok
    and every file in Windows is exactly what it was             ok

  and the restart itself, from firmware, with Secure Boot on
    AurBridge arms it through shim and grub                      ok
    ...the entry starts shim, with no command line of its own    ok
    ...and grub reads a configuration that says whose it is      ok
    the start-up files are on the machine's EFI partition        ok
    the old entry (the kernel itself) is refused under Secure Boot ok
    through shim and grub it installs, and AurOS starts          ok
    ...knowing the disk by its partition table, not the serial   ok
    ...and the installer reported success                        ok

32 checks: with no memory stick, it installs AurOS, starts it,
puts Windows back from the copy on the disk, and every file in Windows
is exactly what it was.
